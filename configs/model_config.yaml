model:
  name: "baseline_random_forest"
  params:
    n_estimators: 100
    max_depth: 10
    random_state: 42
    class_weight: "balanced"

data:
  processed_path: "data/processed/features_ready.csv"
  target_col: "target"
  test_size: 0.2